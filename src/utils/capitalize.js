/**
 * Util function for capitalizing first letter of string
 *
 * @param {string} str string to capitalize
 * @return {string}
 */
export function capitalize(str) {
  return str.charAt(0).toUpperCase() + str.slice(1);
}
